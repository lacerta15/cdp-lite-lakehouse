# CDP-Lite Spark defaults — wires Spark to Hive Metastore + MinIO + Iceberg

# --- Hive Metastore ---
spark.sql.catalogImplementation                  hive
spark.hadoop.hive.metastore.uris                 thrift://hive-metastore:9083
spark.sql.warehouse.dir                          s3a://lakehouse/warehouse/

# --- MinIO (S3A) ---
spark.hadoop.fs.s3a.endpoint                     http://minio:9000
spark.hadoop.fs.s3a.access.key                   minio
spark.hadoop.fs.s3a.secret.key                   minio123
spark.hadoop.fs.s3a.path.style.access            true
spark.hadoop.fs.s3a.connection.ssl.enabled       false
spark.hadoop.fs.s3a.impl                         org.apache.hadoop.fs.s3a.S3AFileSystem

# --- Iceberg ---
spark.sql.extensions                             org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.iceberg                        org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg.type                   hive
spark.sql.catalog.iceberg.uri                    thrift://hive-metastore:9083
spark.sql.catalog.iceberg.warehouse              s3a://lakehouse/iceberg/

# --- Resource defaults (laptop-sized) ---
spark.driver.memory                              1g
spark.executor.memory                            1g
spark.executor.cores                             1

# --- Event log (for Spark History Server, optional) ---
spark.eventLog.enabled                           false
